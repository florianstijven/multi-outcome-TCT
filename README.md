# multi-outcome-TCT

## Unit tests

This project uses `testthat` for unit tests of helper functions in
`R/local-power/helper-functions-local-power.R`.

Run tests from the project root with:

```r
source("tests/testthat.R")
```

Or from a terminal:

```bash
Rscript tests/testthat.R
```